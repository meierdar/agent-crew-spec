# Shared Context: E001

> This file is a living document. Agents append to it as they work.
> Read this BEFORE starting any story in this epic.
> Write to it AFTER making any decision or completing any task.

---

### 2026-03-16T08:06:09Z — db-engineer

Users table: id (uuid, PK), email (varchar, unique), name (varchar), password_hash (varchar), created_at (timestamptz). Using uuid_generate_v4() for IDs. No soft-delete — we'll add that later if needed.

---

### 2026-03-16T08:06:09Z — backend-developer

API contract decided: POST /auth/login takes {email, password}, returns {token, user}. POST /auth/register takes {email, password, name}, returns {user}. All errors use {code, message} shape. Rate limit: 5 attempts per minute on login.

---

### 2026-03-16T08:06:09Z — flutter-developer

Login UI built with email + password TextFormField. Using Form with GlobalKey for validation. Email regex validation, password min 8 chars. Submit button shows CircularProgressIndicator during loading.

---

